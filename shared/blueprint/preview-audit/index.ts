/**
 * `shared/blueprint/preview-audit` barrel（`blueprint-v4-full-alignment` Module E/F）。
 *
 * 汇聚 provenance 产出方（Module F）与出图审计消费方（Module E）的共享类型，
 * 与 `companion` / `traceability-matrix` 子域 barrel 保持一致的导出形态。
 */

export type * from "./types.js";
