/**
 * Autopilot lib 归类（wt2 任务 4，方案 B）：
 *
 * 本文件是 `@/lib/autopilot-launch-examples` 的子路径 re-export 视图。
 * 现有 `import { AUTOPILOT_LAUNCH_EXAMPLES } from "@/lib/autopilot-launch-examples"`
 * 继续有效；新代码推荐使用 `@/lib/autopilot/launch-examples`。
 *
 * 对应 `.kiro/specs/autopilot-blueprint-refactor-split` 需求 1.1、6.5。
 */

export * from "../autopilot-launch-examples.js";
