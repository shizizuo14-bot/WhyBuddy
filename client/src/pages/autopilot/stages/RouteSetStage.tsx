/**
 * Autopilot stage 3: RouteSet。
 *
 * 当前实现：`RouteOption` 组件住在 `../AutopilotRoutePage.tsx`。
 * 物理迁移计划：把 `RouteOption` + RouteSet 列表渲染逻辑搬到本文件。
 *
 * 对应需求 2.5。
 */

export const ROUTESET_STAGE_PLACEHOLDER =
  "see client/src/pages/autopilot/AutopilotRoutePage.tsx (RouteOption)";
