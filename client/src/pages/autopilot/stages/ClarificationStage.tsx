/**
 * Autopilot stage 2: Clarification。
 *
 * 当前实现：`ClarificationPanel` 组件住在 `../AutopilotRoutePage.tsx`（local function）。
 * 物理迁移计划：把 `ClarificationPanel` 搬到本文件，保留 `AutopilotRoutePage` 只作编排。
 *
 * 对应需求 2.5。
 */

export const CLARIFICATION_STAGE_PLACEHOLDER =
  "see client/src/pages/autopilot/AutopilotRoutePage.tsx (ClarificationPanel)";
