/**
 * Autopilot stage 5: Fabric / Execution（预演 + 提示词 + 工程交接）。
 *
 * 当前实现：`AgentCrewSummary` + `AutopilotVisualStage` 等组件住在 `../AutopilotRoutePage.tsx`。
 * 物理迁移计划：把 fabric 阶段面板抽到本文件。
 *
 * 对应需求 2.5。
 */

export const FABRIC_STAGE_PLACEHOLDER =
  "see client/src/pages/autopilot/AutopilotRoutePage.tsx (AgentCrewSummary, AutopilotVisualStage)";
