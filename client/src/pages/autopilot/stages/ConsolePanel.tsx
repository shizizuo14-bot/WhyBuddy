/**
 * Autopilot console 面板（辅助组件）。
 *
 * 当前实现：`AutopilotConsolePanel` 住在 `../AutopilotRoutePage.tsx`（local function）。
 * 物理迁移计划：把 `AutopilotConsolePanel` 与 `buildConsoleLines` helper 搬到本文件。
 *
 * 对应需求 2.5、6.2。
 */

export const CONSOLE_PANEL_PLACEHOLDER =
  "see client/src/pages/autopilot/AutopilotRoutePage.tsx (AutopilotConsolePanel)";
