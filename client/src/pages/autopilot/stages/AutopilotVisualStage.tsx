/**
 * Autopilot 视觉阶段容器（辅助组件）。
 *
 * 当前实现：`AutopilotVisualStage` 住在 `../AutopilotRoutePage.tsx`（local function）。
 * 物理迁移时把它搬进本文件。
 *
 * 对应需求 2.5、6.2。
 */

export const AUTOPILOT_VISUAL_STAGE_PLACEHOLDER =
  "see client/src/pages/autopilot/AutopilotRoutePage.tsx (AutopilotVisualStage)";
