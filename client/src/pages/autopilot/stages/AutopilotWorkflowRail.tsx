/**
 * Autopilot workflow 进度轨道（辅助组件，承载 5 个 stage 的 HUD）。
 *
 * 当前实现：`AutopilotWorkflowRail` 住在 `../AutopilotRoutePage.tsx`（local function）。
 * 物理迁移时把它搬进本文件。
 *
 * 对应需求 2.5、6.2。
 */

export const AUTOPILOT_WORKFLOW_RAIL_PLACEHOLDER =
  "see client/src/pages/autopilot/AutopilotRoutePage.tsx (AutopilotWorkflowRail)";
