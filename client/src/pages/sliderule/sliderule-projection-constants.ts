/** Virtual terminal node id — projection only, never persisted in STATE.graph. */
export const SLIDERULE_TERMINAL_NODE_ID = "sliderule-terminal-delivery";

export type ProjectionDensity = "compact" | "detailed";

export const PROJECTION_DENSITY_STORAGE_KEY = "sliderule:projection-density:v1";